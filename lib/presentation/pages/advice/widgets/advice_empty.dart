import 'package:flutter/material.dart';

class AdviceEmpty extends StatelessWidget {
  const AdviceEmpty({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Du hast noch keine Daten abgerufen, drücke den Knopf unten.');
  }
}
